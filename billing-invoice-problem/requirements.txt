# Mirror of pyproject.toml core dependencies for pip users.
# Prefer: pip install -e ".[dev]"   (installs from pyproject.toml)
duckdb>=1.1
polars>=1.9
pandas>=2.2
numpy>=1.26
pyarrow>=17.0
scikit-learn>=1.5
lightgbm>=4.5
shap>=0.46
matplotlib>=3.9
pydantic>=2.9
pydantic-settings>=2.5
pyyaml>=6.0

# --- dev ---
pytest>=8.3
pytest-cov>=5.0
ruff>=0.7

# --- optional serving (install explicitly if needed) ---
# fastapi>=0.115
# uvicorn[standard]>=0.32
